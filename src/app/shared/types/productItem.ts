export type ProductItem = {
    id: number;
    name: string;
    price: number;
    imageUrl: string;
    quantity: number;
}

export type BlogItem = {
    id?: number;
    title?: string;
    body?: string;
    author?: string;
}
